on:
  pull_request:
    

jobs:
  run_if:
    if:  contains(github.head_ref, 'molly') || contains(github.head_ref, 'something')
    runs-on: ubuntu-latest
    env: 
      PROJECT: cto-sic-annotation-tool-d
      APPENV: staging
    steps:
    - name: get base
      run: |
            echo  ${{ github.head_ref }}
