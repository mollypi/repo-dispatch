name: Repository Dispatch
on:
  repository_dispatch:
    types:
      - my-event
jobs:
  myEvent:
    runs-on: ubuntu-latest
    env:
      STUFF: '${{ toJson(github.event.client_payload) }}'
    steps:
      - name: test
        run: echo $STUFF
