

name: testdispatch

on:
  workflow_dispatch:
  
  
jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
     - name: checkout code
       uses: actions/checkout@v2
       
     - name: check for changes first
       id: checkforchange
       run: echo "$GITHUB_ENV"
          
