on:
  issue_comment:
    types:
    - created

jobs:
  runjob:
    runs-on: ubuntu-latest
    steps:

      - name: checkout code 
        uses: actions/checkout@v2
      
      - name: Get current tag
        id: get-current-tag
        run: echo ${{ github.ref }} 

